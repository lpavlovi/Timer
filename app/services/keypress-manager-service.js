'use strict';
angular.module('rootModule')
  .service('keyManager', function() {
    // stuff
    var keyValuePair = [];
    this.add = function(x) {
      masterArray.push(settings);
    };
    this.remove = function(i) {
      masterArray.push(settings);
    };
});
